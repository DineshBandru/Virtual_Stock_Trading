{
  "name": "virtual-stock-platform",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix apps/website run dev\" \"npm --prefix apps/admin run dev\"",
    "build": "npm --prefix apps/website run build",
    "start": "npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "lucide-react": "^1.17.0"
  }
}
